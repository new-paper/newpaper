#set page(paper: "a4",margin: 1cm)
#set text(size: 14pt)

#set heading(numbering: "1.1")

$ frac(x,y,style: "vertical") $
$ frac(x,y,style: "skewed") $
$ frac(x,y,style: "horizontal") $

= CONCEAL
$
frac(pi^alpha,pi_(cal(X))) + frac(pi_a,pi^1) + frac(pi^cal("XYZ"),pi_(alpha_1))

frac(pi^(cal(X)_1),pi_(a_1)) + frac(pi_(1_1),pi^(alpha_a)) + frac(pi^(cal(X)_a),pi_(a_a))

frac(pi_(1_a),pi^(alpha^1)) + frac(pi^(a^1),pi_(1^1)) + frac(pi_(cal(X)^1),pi^(alpha^a))

frac(pi^(a^a),pi_(1^a)) + frac(pi_(cal(X)^a),pi^(cal("XYZ")^1))

frac(pi^(cal("XYZ")_1),pi_(cal("XYZ")^a)) + frac(pi_(cal("XYZ")_a),pi^(cal("XYZ")^1))

frac(pi^(cal("XYZ")_1),pi_(cal("XYZ")^a)) + frac(pi_(cal("XYZ")_a),pi^(cal("ABC")_a))

frac(cal("XYZ"),cal("ABC")^a) + frac(cal("XYZ"),cal("ABC")_a)

frac(frac(alpha^phi,2),pi) + frac(alpha_phi,2) + frac(alpha^2,2)

frac(frac(alpha,alpha),frac(alpha,alpha))

frac(1,frac(3,4)) + frac(a^2,b^3) + frac(cal(X),phi) + frac(cal(X)^2,cal(Y)_a)

frac(3,2^2) + frac(3,pi^2)

frac(1,2) + frac(1_2,3^4) + frac(1,2^3) + frac(1,2_a) + frac(1^2,3) + frac(1_a,2)

frac(frac(1,2),frac(1,frac(3,4))) => (1/2)/(1/(3/4))

frac(frac(1,2),frac(1,2)) => (1/2)/(1/2)

frac(a,c + d) => a/(c + d)

frac(a,c) + d => (a/c) + d

frac(frac(a+b,c+d),frac(a+b,c+d))

frac(a+b,c+d)
$
= CONCEAL with inner `(` and `)`
$
frac((14 + 4.427  9.585),(4 dot 5)) => frac(14 + 4.427  9.585,4 dot 5)

frac((a+b),(c+d)) => frac(a+b,c+d)

frac((x^2 + 1),(x^2 - 1)) => frac(x^2 + 1,x^2 - 3^2)

frac((x^2 + 1),x^2) => frac(x^2 + 1,x^2)

frac(1,(1 + frac(1,2))) => frac(1,1 + frac(1,2))

frac(1,(2 + frac(1,(3 + frac(1,(a + 4)))))) => frac(1,2 + frac(1,3 + frac(1,a + 4)))
$
= CONCEAL partially with nested level limits
$
frac(frac(frac(frac(1,2),frac(3,4)),frac(frac(5,6),frac(7,8))) + frac(frac(frac(1,2),frac(3,4)),frac(frac(5,6),frac(7,8))),frac(frac(frac(1,2),frac(3,4)),frac(frac(5,6),frac(7,8))) + frac(frac(frac(1,2),frac(3,4)),frac(frac(5,6),frac(7,8))))

frac(frac(frac(1,2),frac(3,4)),frac(frac(5,6),frac(7,8)))

frac(frac(1,frac(3,4)),frac(1,frac(3,4)))
$
