#set page(paper: "a4", margin: 1cm)
#set text(font: ("STIX", "STIX Two Text"))
#set text(size: 16pt)
= Blackboard Bold (bb)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$bb(A)$],            [𝔸],        [$bb(a)$],                [𝕒],
  [$bb(Alpha)$],        [$ Α $],    [$bb(alpha)$],            [$ 𝛼 $],
  [$bb(epsilon)$],      [$ 𝜀 $],    [$bb(epsilon.alt)$],      [$ 𝜖 $],
  [$bb(0)$],            [𝟘],        [],                       [],
)

#pagebreak()

= Bold

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$bold(A)$],          [𝐀],        [$bold(a)$],              [𝐚],
  [$bold(Alpha)$],      [𝚨],        [$bold(alpha)$],          [𝜶],
  [$bold(epsilon)$],    [$ 𝜺 $],    [$bold(epsilon.alt)$],    [$ 𝝐 $],
  [$bold(0)$],          [𝟎],        [],                       [],
)

#pagebreak()

= Calligraphic (cal)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$cal(A)$],           [𝒜],        [$cal(a)$],               [𝒶],
  [$cal(Alpha)$],       [$ Α $],    [$cal(alpha)$],           [$ 𝛼 $],
  [$cal(epsilon)$],     [$ 𝜀 $],    [$cal(epsilon.alt)$],     [$ 𝜖 $],
  [$cal(0)$],           [𝟶],        [],                       [],
)

#pagebreak()

= Fraktur (frak)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$frak(A)$],          [𝔄],        [$frak(a)$],              [𝔞],
  [$frak(Alpha)$],      [$ Α $],    [$frak(alpha)$],          [$ 𝛼 $],
  [$frak(epsilon)$],    [$ 𝜀 $],    [$frak(epsilon.alt)$],    [$ 𝜖 $],
  [$frak(0)$],          [𝟶],        [],                       [],
)

#pagebreak()

= Italic (italic)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$italic(A)$],        [𝐴],        [$italic(a)$],            [𝑎],
  [$italic(Alpha)$],    [$ 𝛢 $],    [$italic(alpha)$],        [$ 𝛼 $],
  [$italic(epsilon)$],  [$ 𝜀 $],    [$italic(epsilon.alt)$],  [$ 𝜖 $],
  [$italic(0)$],        [𝟶],        [],                       [],
)

#pagebreak()

= Monospace (mono)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$mono(A)$],          [𝙰],        [$mono(a)$],              [𝚊],
  [$mono(Alpha)$],      [$ Α $],    [$mono(alpha)$],          [$ 𝛼 $],
  [$mono(epsilon)$],    [$ 𝜀 $],    [$mono(epsilon.alt)$],    [$ 𝜖 $],
  [$mono(0)$],          [𝟶],        [],                       [],
)

#pagebreak()

= Sans-serif (sans)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$sans(A)$],          [𝖠],        [$sans(a)$],              [𝖺],
  [$sans(Alpha)$],      [$ 𝝖 $],    [$sans(alpha)$],          [$ 𝞪 $],
  [$sans(epsilon)$],    [$ 𝛆 $],    [$sans(epsilon.alt)$],    [$ 𝛜 $],
  [$sans(0)$],          [𝟢],        [],                       [],
)

#pagebreak()

= Script (scr)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$scr(A)$],           [𝒜],        [$scr(a)$],               [𝒶],
  [$scr(Alpha)$],       [$ Α $],    [$scr(alpha)$],           [$ 𝛼 $],
  [$scr(epsilon)$],     [$ 𝜀 $],    [$scr(epsilon.alt)$],     [$ 𝜖 $],
  [$scr(0)$],           [𝟶],        [],                       [],
)

#pagebreak()

= Serif (serif)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$serif(A)$],         [𝐴],        [$serif(a)$],             [𝑎],
  [$serif(Alpha)$],     [$ Α $],    [$serif(alpha)$],         [$ 𝛼 $],
  [$serif(epsilon)$],   [$ 𝜀 $],    [$serif(epsilon.alt)$],   [$ 𝜖 $],
  [$serif(0)$],         [0],        [],                       [],
)

= Upright (upright)

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Latin Upper*],      [*Symbol*], [*Latin Lower*],          [*Symbol*],
  [$upright(A)$],       [A],        [$upright(a)$],           [a],
  [$upright(Alpha)$],   [$ Α $],    [$upright(alpha)$],       [$ α $],
  [$upright(epsilon)$], [$ ε $],    [$upright(epsilon.alt)$], [$ ϵ $],
  [$upright(0)$],       [0],        [],                       [],
)

#pagebreak()

= acute

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$acute(A)$], [Á], [$acute(a)$], [á],
  [$acute(Alpha)$], [Ά], [$acute(alpha)$], [ά],
  [$acute(epsilon)$], [έ], [$acute(epsilon.alt)$], [ϵ́],
  [$acute(0)$], [0́], [], [],
)

= acute.double

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$acute.double(A)$], [A̋], [$acute.double(a)$], [a̋],
  [$acute.double(Alpha)$], [Α̋], [$acute.double(alpha)$], [α̋],
  [$acute.double(epsilon)$], [ε̋], [$acute.double(epsilon.alt)$], [ϵ̋],
  [$acute.double(0)$], [0̋], [], [],
)

= arrow

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$arrow(A)$], [A⃗], [$arrow(a)$], [a⃗],
  [$arrow(Alpha)$], [Α⃗], [$arrow(alpha)$], [α⃗],
  [$arrow(epsilon)$], [ε⃗], [$arrow(epsilon.alt)$], [ϵ⃗],
  [$arrow(0)$], [0⃗], [], [],
)

= arrow.l

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$arrow.l(A)$], [A⃖], [$arrow.l(a)$], [a⃖],
  [$arrow.l(Alpha)$], [Α⃖], [$arrow.l(alpha)$], [α⃖],
  [$arrow.l(epsilon)$], [ε⃖], [$arrow.l(epsilon.alt)$], [ϵ⃖],
  [$arrow.l(0)$], [0⃖], [], [],
)

= arrow.l.r

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$arrow.l.r(A)$], [A⃡], [$arrow.l.r(a)$], [a⃡],
  [$arrow.l.r(Alpha)$], [Α⃡], [$arrow.l.r(alpha)$], [α⃡],
  [$arrow.l.r(epsilon)$], [ε⃡], [$arrow.l.r(epsilon.alt)$], [ϵ⃡],
  [$arrow.l.r(0)$], [0⃡], [], [],
)

#pagebreak()

= breve

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$breve(A)$], [Ă], [$breve(a)$], [ă],
  [$breve(Alpha)$], [Ᾰ], [$breve(alpha)$], [ᾰ],
  [$breve(epsilon)$], [ε̆], [$breve(epsilon.alt)$], [ϵ̆],
  [$breve(0)$], [0̆], [], [],
)

= caron

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$caron(A)$], [Ǎ], [$caron(a)$], [ǎ],
  [$caron(Alpha)$], [Α̌], [$caron(alpha)$], [α̌],
  [$caron(epsilon)$], [ε̌], [$caron(epsilon.alt)$], [ϵ̌],
  [$caron(0)$], [0̌], [], [],
)

= circle

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$circle(A)$], [Å], [$circle(a)$], [å],
  [$circle(Alpha)$], [Α̊], [$circle(alpha)$], [α̊],
  [$circle(epsilon)$], [ε̊], [$circle(epsilon.alt)$], [ϵ̊],
  [$circle(0)$], [0̊], [], [],
)

= dash

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$dash(A)$], [A̅], [$dash(a)$], [a̅],
  [$dash(Alpha)$], [Α̅], [$dash(alpha)$], [α̅],
  [$dash(epsilon)$], [ε̅], [$dash(epsilon.alt)$], [ϵ̅],
  [$dash(0)$], [0̅], [], [],
)

= diaer

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$diaer(A)$], [Ä], [$diaer(a)$], [ä],
  [$diaer(Alpha)$], [Α̈], [$diaer(alpha)$], [α̈],
  [$diaer(epsilon)$], [ε̈], [$diaer(epsilon.alt)$], [ϵ̈],
  [$diaer(0)$], [0̈], [], [],
)

#pagebreak()

= dot

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$dot(A)$], [Ȧ], [$dot(a)$], [ȧ],
  [$dot(Alpha)$], [Α̇], [$dot(alpha)$], [α̇],
  [$dot(epsilon)$], [ε̇], [$dot(epsilon.alt)$], [ϵ̇],
  [$dot(0)$], [0̇], [], [],
)

= dot.double

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$dot.double(A)$], [Ä], [$dot.double(a)$], [ä],
  [$dot.double(Alpha)$], [Α̈], [$dot.double(alpha)$], [α̈],
  [$dot.double(epsilon)$], [ε̈], [$dot.double(epsilon.alt)$], [ϵ̈],
  [$dot.double(0)$], [0̈], [], [],
)

= dot.quad

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$dot.quad(A)$], [A⃜], [$dot.quad(a)$], [a⃜],
  [$dot.quad(Alpha)$], [Α⃜], [$dot.quad(alpha)$], [α⃜],
  [$dot.quad(epsilon)$], [ε⃜], [$dot.quad(epsilon.alt)$], [ϵ⃜],
  [$dot.quad(0)$], [0⃜], [], [],
)

= dot.triple

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$dot.triple(A)$], [A⃛], [$dot.triple(a)$], [a⃛],
  [$dot.triple(Alpha)$], [Α⃛], [$dot.triple(alpha)$], [α⃛],
  [$dot.triple(epsilon)$], [ε⃛], [$dot.triple(epsilon.alt)$], [ϵ⃛],
  [$dot.triple(0)$], [0⃛], [], [],
)

= grave

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$grave(A)$], [À], [$grave(a)$], [à],
  [$grave(Alpha)$], [Ὰ], [$grave(alpha)$], [ὰ],
  [$grave(epsilon)$], [ὲ], [$grave(epsilon.alt)$], [ϵ̀],
  [$grave(0)$], [0̀], [], [],
)

#pagebreak()

= harpoon

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$harpoon(A)$], [A⃑], [$harpoon(a)$], [a⃑],
  [$harpoon(Alpha)$], [Α⃑], [$harpoon(alpha)$], [α⃑],
  [$harpoon(epsilon)$], [ε⃑], [$harpoon(epsilon.alt)$], [ϵ⃑],
  [$harpoon(0)$], [0⃑], [], [],
)

= harpoon.lt

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$harpoon.lt(A)$], [A⃐], [$harpoon.lt(a)$], [a⃐],
  [$harpoon.lt(Alpha)$], [Α⃐], [$harpoon.lt(alpha)$], [α⃐],
  [$harpoon.lt(epsilon)$], [ε⃐], [$harpoon.lt(epsilon.alt)$], [ϵ⃐],
  [$harpoon.lt(0)$], [0⃐], [], [],
)

= hat

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$hat(A)$], [Â], [$hat(a)$], [â],
  [$hat(Alpha)$], [Α̂], [$hat(alpha)$], [α̂],
  [$hat(epsilon)$], [ε̂], [$hat(epsilon.alt)$], [ϵ̂],
  [$hat(0)$], [0̂], [], [],
)

= macron

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$macron(A)$], [Ā], [$macron(a)$], [ā],
  [$macron(Alpha)$], [Ᾱ], [$macron(alpha)$], [ᾱ],
  [$macron(epsilon)$], [ε̄], [$macron(epsilon.alt)$], [ϵ̄],
  [$macron(0)$], [0̄], [], [],
)

= overline

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$overline(A)$], [A̅], [$overline(a)$], [a̅],
  [$overline(Alpha)$], [Α̅], [$overline(alpha)$], [α̅],
  [$overline(epsilon)$], [ε̅], [$overline(epsilon.alt)$], [ϵ̅],
  [$overline(0)$], [0̅], [], [],
)

#pagebreak()

= tilde

#table(
  columns: 4,
  align: center,
  stroke: 0.5pt,
  [*Render*], [*Unicode*], [*Render*], [*Unicode*],
  [$tilde(A)$], [Ã], [$tilde(a)$], [ã],
  [$tilde(Alpha)$], [Α̃], [$tilde(alpha)$], [α̃],
  [$tilde(epsilon)$], [ε̃], [$tilde(epsilon.alt)$], [ϵ̃],
  [$tilde(0)$], [0̃], [], [],
)
